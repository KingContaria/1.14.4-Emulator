package me.contaria.emulator114.plugin.annotations;


/**
 * Used for annotating Mixin methods that are only used as a temporary solution until I find a nicer implementation.
 * For example when using an Overwrite that could probably be replaced by something less intrusive.
 */
public @interface Ugly {
}
